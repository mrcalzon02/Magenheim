{
  "version": "0.0.60",
  "description": "0.0.60: Closes the five crystal tier meshes. They shipped as triangle soups with over a thousand open edges each, and they are the models the whole refinement loop handles. The last unwired asset gate now runs in the build."
}
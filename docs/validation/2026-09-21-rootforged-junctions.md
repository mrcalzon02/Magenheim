# Rootforged junction expansion â€” 0.0.93

Adds Worldroot Y Brace 4m (4 x 4 x 1m), T Brace 4m (4 x 2 x 1m), and Forked Column 8m (4 x 8 x 1m). Workbench recipes cost 6, 4 and 12 core wood respectively. Appended catalog enum values preserve existing serialized identities. All existing foundation definitions were checked semantically unchanged.

Each piece has three braided strands and two fine root veins per limb, packed textures, editable Blender source, exported GLB/runtime payload and matching rendered Hammer icon. Existing native HardWood, collision, wear states, network identity and material refund behavior are reused. Branch pieces have direct-child base, junction and branch-tip snap points.

Asset checks verify all 14 catalog bindings, dimensions, ground contact and collision. New branch checks require both upper arms, open lower clearance and braided trunks. Geometry checks passed for all three new models, including winding. Source icon contact sheet reviewed at artifacts/review/rootforged/rootforged-junctions.png.

In-game placement, snapping, collapse and multiplayer acceptance remain untested.

Full closeout succeeded: 297 model sets, 131 icons, 38,326 Core assertions plus separately reported modules. Runtime build had zero warnings/errors. The loader regression now compares the shipped architecture fingerprint to the canonical catalog, replacing the obsolete eleven-piece assumption that stopped the first run.

Installed version 0.0.93 and verified all installed payload hashes plus enabled launcher metadata. DLL SHA-256: 7D821C0EA5549CE470F46C8D298E8BA5DCCCA54C52E7970282D961D4B657A3ED. Previous installation: backups/Local-Magenheim-20260921-210908.zip; launcher catalog: backups/mods-20260921-210916-543.yml. Full local log: dist/rootforged-junction-closeout.log.
